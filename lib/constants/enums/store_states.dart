enum StoreStates { initial, loading, locked, success, error, empty }

enum KadenaWallet { koala, eckoWallet, chainweaver, zelcore, linx, ledger }

enum DerivationMethod { bip32, bip44, bip49, bip84, bip141 }
